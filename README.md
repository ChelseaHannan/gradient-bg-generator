# gradient-bg-generator
Generates 2 random colors w/ the click of a button and produces a gradient background with them. Displays rgba values for colors. Allows you to manually pick 2 colors and set background.
